
```
$ express -e photo
 change directory:
     $ cd photo
   install dependencies:
     $ npm install
   run the app:
     $ DEBUG=photo:* npm start
     热加载不需要重启:
     $ DEBUG=photo:* npm run dev
```
